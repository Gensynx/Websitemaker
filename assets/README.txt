darulamir-intro.mp4 — the single scroll-scrubbed intro clip used on all viewports.
2560x1440, all-intra (a keyframe on every frame) so scroll-scrubbing can seek to
any position and decode one frame instantly. object-fit:cover center-crops it on
portrait phones; the motion converges on the central door, so the crop holds.
